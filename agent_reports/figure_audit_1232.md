## Figure audit — 12:32

**Scope:** First pass over all five explainer figures (`explainer_cf_her`, `explainer_family_tree`, `explainer_problem`, `explainer_semantic_per`, `explainer_verified_cf`), plus a fresh-eyes check of the previously-improved paper figure `fig_morning_headline`. Paper-grade figures `fig1_headline_success`, `fig4_averages`, `figN2_cross_task_transfer`, `figN_c1v2_model_comparison`, `figN_c1v2_real_vs_synthetic` were all reviewed previously and are in acceptable shape — no changes made this cycle.

**Action taken:** `explainer_cf_her.png` had a bold in-canvas chart title ("Counterfactual HER: 'what if the goal had been here?'") that violated the NeurIPS "no chart titles" convention stored in memory. The title was removed from `make_explainer_figs.py` (function `fig_cf_her`) and replaced with a comment noting the caption text belongs in the surrounding PDF page heading. With the title gone, `traj_y` baseline was shifted from 3.5 to 3.8 so the trajectory uses the freed vertical space and sits less cramped above the warning callout box. Figure was regenerated via `.venv` at 300 DPI. Script: `/home/daniel-grant/Berkeley/Spring2026/DeepRL/FinalProject/RL_project_185/agent_reports/make_explainer_figs.py`.

**Remaining minor flags (no action taken this cycle):** `explainer_verified_cf.png` has a bold in-canvas title ("Verified-CF (our Path C, agent N1)") — same NeurIPS violation, lower priority since this figure is more self-contained and the title serves a navigational purpose in the multi-page explainer PDF. Could be addressed in a future cycle if the PDF layout is revised. `explainer_family_tree.png` is a diagram (no title, appropriate). `explainer_problem.png` and `explainer_semantic_per.png` use bold in-canvas structural labels (episode labels, not chart titles) — these are acceptable.
