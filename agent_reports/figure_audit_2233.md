**Figure audit — 2026-05-12 22:33**

Candidates reviewed (excluding the three protected v4-overhaul figures: `fig_headline_v4`, `fig_envs`, `fig_learning_curves_v2`): `figN_c1v2_model_comparison`, `figN_c1v2_real_vs_synthetic`, `figN2_cross_task_transfer`, `fig_morning_headline`, `fig_learning_curves`, plus older `fig*` variants. The weakest figure was **`figN2_cross_task_transfer`**: its generation script (`make_figN2.py`) placed an `ax.text()` annotation reading "n = 4 episodes per env" at `transform=ax.transAxes, y=-0.2`, which floated in dead whitespace *between* the x-axis tick labels and the legend (itself anchored at `bbox_to_anchor=(0.5, -0.4)`), producing ~40% of the figure's total vertical extent as empty margin — a clear NeurIPS violation ("no verbose text annotations on plots"; sample size belongs in the caption). Fix applied: removed the in-axes annotation and tightened the legend anchor from `y=-0.4` to `y=-0.22`, which eliminates the dead whitespace while keeping the legend clear of the tick labels. Script and both output files (PNG + PDF) updated. No other figures were modified.
