Generated `fig_headline_v5` (PNG + PDF) by updating the HER FetchPickAndPlace bar from the stale 250k baseline (mean 0.183, seeds [0.10, 0.05, 0.40]) to the just-completed HER@1M run (seeds s42=0.35, s123=0.45, s999=0.95, mean 0.583, SE≈0.18; verified W&B + commit 06cc808). The horizon stamp for that cell was changed from "250k" to "1M", the legend entry updated to "HER (1M (PnP) / 250k†)", and the HER dashed reference line for FetchPickAndPlace now sits at 0.583 (co-incident with Oracle-CF@1M PnP at 0.583 — a notable finding the figure now makes legible). FetchPush and FetchSlide HER bars remain at their 250k values with a "250k†" stamp since those @1M runs are still in flight (ETA ~04:30); the footnote annotation was updated to flag this. All other data, the CB-safe palette, seed-dot overlay, ±1 SE caps, and per-bar horizon stamps are preserved identically from v4. Visual inspection of the rendered PNG confirmed the PnP HER bar now stands at ~0.58 and the reference line aligns with Oracle-CF. Both paper `includegraphics` references (NeurIPS and CS285 variants) were updated from `fig_headline_v4.pdf` to `fig_headline_v5.pdf`, and figure files copied into both paper directories. v4 preserved untouched as reference.
