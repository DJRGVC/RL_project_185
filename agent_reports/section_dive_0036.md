# Section dive 0036 — §5.3 prompt-design consolidation

## What changed

Replaced the single closing paragraph of §5.3 ("Connection to the
IS-posterior framing") in `agent_reports/paper/main.tex` with three
paragraphs that consolidate three conceptual threads that had
accumulated in adjacent sections over the past 24+ hours but were not
yet woven into §5.3 itself (§5.3 was the least-touched substantive
subsection, ~28+ hours stale at the start of the dive). Specifically:
(1) the original IS-posterior paragraph is retained as paragraph one
but tightened to name the IS-pairing requirement of §2.2/§3 explicitly,
not just the trajectory-conditional structure of $q_\phi$; (2) a new
paragraph ("From a one-axis screen to a $2\!\times\!2$ structural
grid") factors the $\textsc{achieved\_goal}$ vs $\textsc{all}$
single-axis screen into the two independent axes (goal-visibility,
trajectory-context) that §5.6(G) pre-registers as a $2\!\times\!2$
ablation, framing the cross-link in §2.2/§3 language (which
prompt-design axes preserve $\tau$-conditionality of $q_\phi$); (3) a
new paragraph ("A second empirical test: the $p_{\text{CF}}$ sweep")
states the multiplicative-IS form's quantitative prediction—that an
imperfect VLM proposal should degrade monotonically with $p_{\text{CF}}$
above a small-injection plateau—and reports the partial 2-of-6 read
($p\!=\!0.00 \approx p\!=\!0.25$, $p\!\ge\!0.75$ tracking $0.04$–$0.10$)
as a directional test with a pre-registered falsifier (any
$p\!\ge\!0.50$ cell exceeding $p\!=\!0.25$ by $>\!1$ SE at full
$n\!=\!3$). The full 18-run reading is deferred to camera-ready and
the partial result is footnote-flagged as such.

## Build / page count / safety

Page count: **48 → 49** (still under the 50-page NeurIPS limit;
~+1 column of slack remaining). Build is clean modulo the pre-existing
`tab:vlm_comparison` undefined-reference warning, which is orthogonal
and dates to before this session. No new figures, no bibliography
changes, no new claims advanced beyond the partial data we already
have. The CS 285 paper (`agent_reports/paper_cs285/main.tex`) is
untouched and remains locked at 11 pages. Numbers were sourced from
`agent_reports/_psweep_results.md` (auto-generated by
`make_psweep_analysis.py`) and cross-referenced to §5.6(G) and §6(vii),
which already cite the same in-flight sweep — so the new §5.3 text is
internally consistent with the rest of the paper and does not
introduce any independently-validating numbers. Commit on
`agent/pathc-lead`, pushed to `main` via
`git push origin agent/pathc-lead:main`.
