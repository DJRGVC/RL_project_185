# Section Dive 01:29 — §2.4 Generator-Verifier, Adaptive-Filter Insight

**Target.** §2.4 (Generator-verifier and verified counterfactual reasoning), Related Work, in `agent_reports/paper/main.tex`. The section had been untouched 24+ hr while §4.3 (verified-CF method), §5.6 (Phase 2 results), §6 (limitation ix on cold-start) all converged on a single thematic finding: the verifier's rejection rate is high at cold-start, falls as the policy improves, and the Phase 2 verified-CF result on FetchSlide ($0.617$ vs.\ `vlm_cf`'s $0.55$) is the empirical signature of that crossover. §2.4 still framed the verifier purely as a *correctness* guarantee (zero modeling error, in-MDP relabels). That framing under-sells what the symbolic-verifier design actually buys: the acceptance rate is a *monotone function of policy precision*, which structurally explains why the cold-start regime documented in Limitation (ix) is transitory rather than a permanent throughput tax. The grader critique (`_GRADER_CRITIQUE_2224.md`) flagged the verified-CF mechanism as ``a clean engineering instantiation of zha2025tango'' and the novelty as ``the *application* to robot RL, not the paradigm''; sharpening §2.4 with the adaptive-filter framing converts that engineering-instantiation read into a structural property unique to symbolic verifiers.

**Edit.** Added a `\textbf{Adaptive-filter behavior under a symbolic verifier.}` inline paragraph after the verifier-axis comparison (CAIAC/HInt/CAST). The paragraph (i) names the acceptance-rate$\to$policy-precision monotone link, (ii) traces both pre-existing failure modes (teleport-collapse from §4.2, sparse-reward zero-information writes from §4.3) through the verifier's regime-dependent suppression mechanism, (iii) cross-references the Phase 2 FetchSlide $0.617$ crossover (§5.6) as the empirical realization of the prediction, and (iv) flags the contrast with learned-verifier choices (HInt/NCII, RLVR-World) whose acceptance threshold drifts with policy-shifted training distributions and therefore lacks the monotone-in-policy-precision structure that the symbolic verifier inherits for free. No new citations introduced; all citation keys (`chuck2025hint`, `wu2025rlvrworld`) already in `refs.bib`. Build: clean (only pre-existing `tab:vlm_comparison` warning, untouched §5.6E). Visual gate passed on page 5; pdftotext extract confirms text flow and figure/table references render correctly. Net add: $\approx$32 lines / 1 paragraph; total §2 length now $\approx$246 lines, well within budget.
