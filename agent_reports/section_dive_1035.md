# Section dive 10:35 — §4.3 VLM-Verified Counterfactual Hindsight, mechanistic story

**Target.** §4.3 (`sec:method:verified`, main.tex lines 836–960 post-edit) was the best least-touched candidate per the morning audit: untouched in last 10 hr except for the late-October smoke-test paragraph and the unrelated CALM addition. R1's W11/W6 critique is that VCH is described as a beautiful mechanism but evaluated only as a 4/4 unit test, with no RL training curves. The Modal Phase 2 retry (claude-sonnet-4-5, 250k-horizon SAC+HER+VCH) will eventually fix the empirical gap; in the meantime the *mechanistic prose* could be sharpened without depending on incoming data. Two new paragraphs were inserted between the smoke-test paragraph and the existing "generator–verifier framing" paragraph, plus a forward-reference sentence to Phase 2 was appended to the smoke-test paragraph, and a stale cross-reference (`sec:method:verified-cf` → `sec:method:verified` on line 1409) was repaired.

**Changes.** (1) New paragraph "Why plain CF-HER is mechanistically insufficient under sparse reward" makes explicit the load-bearing failure mode that motivates the verifier: on a typical C1 episode the VLM's corrective action ends at $0.32$\,m or $0.16$\,m, never crossing the $d_{\text{th}}=0.05$\,m threshold, so the unverified-CF transition writes the same $r_T=-1$ as the original failure into the buffer — HER absorbs the only positive signal and the CF channel contributes variance, not learning. This converts a previously implicit story (the verifier "closes a loophole") into an explicit zero-information-write argument. (2) New paragraph "Calibrated posterior under the verifier gate" links §4.3 to §3's IS-posterior assumption A5: accepted counterfactuals are tuples where the implicit $q_\phi(a_{K:K+N}\mid\tau)$ is a point mass on the verified action under which the env's own reward predicate fires, i.e. $\varepsilon_{\text{cal}}=0$ in Assumption A5 (Appendix C.3). The two contributions are then stacked consistently: Semantic PER tolerates miscalibration as bounded variance (Eq. bias-bound); the verifier converts miscalibration into rejection-rate overhead instead of biased buffer writes — "in the Semantic-PER stack calibration is a quality dial; in the verifier stack it is a throughput dial." This is the cleanest way to integrate the verifier with the theory section without making claims that depend on the in-flight Modal data. (3) Forward-reference sentence in the smoke-test paragraph names the smoke result as a unit test (not an RL result) and points at the 250\,k-step Phase 2 sweep as the prospective adjudication, which addresses R1 W11's "promissory text in lieu of an adjudicated result" framing honestly. Build: clean first try, 36 pages, 0 overfull/underfull boxes, 0 warnings; all new cross-refs (Section 3, Section 4.1, Section 4.3, Appendix C.3, Eq. bias-bound, sec:exp:inflight) resolve. No new figures, no bibliography churn, no conflicts with in-flight training (local Oracle-CF 1M PIDs 199586/199587 or Modal Phase 2 retry) or with paper_iter cron (:13 and :38 — work finished at 10:35, two-minute cushion before :38). Commit on `agent/pathc-lead`.
