# Section dive — 14:35 PDT, 2026-05-12

## Target picked: §4.2 (Counterfactual Prompting and the Teleport-Collapse Failure Mode), main.tex lines ~739–843

Out of the prompt's least-touched candidate list — §4.2 (15+ hrs stale), Appendix E (already meaningfully populated by prior commits and labeled `app:repro`), Appendix F (just refreshed at `c488c6b` for the in-paper §6 broader-impacts paragraph and only ~30 lines long) — §4.2 was the highest-leverage non-data section. Its most recent substantive touch was `bbdee7e` at 23:35 yesterday (the original "deep improvement" that introduced the four-variant taxonomy, the Dirac formalization of teleport-collapse, and the mechanistic position-copy argument). Since then, two structural pieces have been added to the rest of the paper that did not feed back into §4.2: the bias bound `eq:bias-bound` (commit `95d9cee`) and the formal two-regime $q_\phi$-degeneracy split in Appendix B.3 (`9c75ab3`, today 13:35). §4.2 named the Dirac $q_\phi(\hat{p}\!\mid\!\tau)\!=\!\delta(\hat{p}\!-\!g)$ inside the teleport-collapse Definition box but did not map any of the four variants onto the two-regime taxonomy that the appendix now makes formal. A reader of §4.2 alone could not tell whether \textsc{narrative}/\textsc{achieved\_goal}/\textsc{all}/\textsc{action} lived in the "variance-amplification" regime (Sem-PER channel) or the "signal-extinction" regime (verifier channel), even though §4.2's whole purpose is to defend the variant choice for the verifier of §4.3. Inserted a 1-paragraph "Mapping variants to $q_\phi$-degeneracy regimes" between the mechanistic account and the verifier-as-gate paragraph that gives the four-by-two map explicitly: \textsc{narrative} ⇒ $m_{\text{gen}}\!=\!0$ on the action channel (modality-degenerate); position-emitting variants ⇒ Dirac $q_\phi$, $m_{\text{ver}}\!\to\!0$; \textsc{action} ⇒ non-degenerate $q_\phi$. This makes the §4.2-to-B.3 link visible in the main body for any reader who reads §4.2 without flipping to the appendix, which is the main R2 (theory-side) failure pattern that prior section-dives have not addressed for the prompt-design paragraphs specifically.

## Secondary fix: R1 W9(iv) on the bidirectional variant

R1 explicitly flagged that the bidirectional control's success-timestep $t_{\text{succ}}$ definition — "the centre of the window over which $\|\text{ag}_t\!-\!g\|$ dropped the most" — leaves the window length undefined. Tightened to an explicit $\arg\max_t(\|\text{ag}_{t-W_s}\!-\!g\| - \|\text{ag}_{t+W_s}\!-\!g\|)$ with sliding half-width $W_s\!=\!W\!=\!5$ inherited from the failure kernel (symmetric-by-design rather than separately tuned, which is a defensible reproducibility statement that does not require a new hyperparameter sweep). Tie-break toward later $t$ specified so the choice is well-defined when the trajectory has a long flat run. This is the kind of "method clarity / reproducibility gap" R1 W9 collected; closing one of its sub-items via main-body tightening (rather than via the hyperparameter table) is the right level of fix. Compile gate clean: 40 pages, 598511 bytes, no new warnings; the only "undefined references" entry in the log is the pre-existing `tab:vlm_comparison` reference at main.tex:1537 which sits outside this edit region. Committed at `71c68da` on `agent/pathc-lead` at 14:35 PDT, comfortably before the 14:38 paper-iter slot.
