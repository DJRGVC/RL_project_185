# Section Dive 1535 — Appendix C.1 (was B.1): IS Derivation Tightening

**Target.** `appendix.tex` §C.1 "Importance-sampled posterior reweighting --- full derivation" (`\label{app:is-derivation}`), the foundational theory appendix referenced from §3 main text. Chosen over the §3 setup paragraph (recently touched, downstream eqref risk) and §5.3 (touched 02:37 today) for being least-recently-edited and most R2-leverage. No equation labels added or removed; no cross-references broken. Compile clean at 41 pages; only pre-existing undefined ref (`tab:vlm_comparison`).

**Substantive changes.** (1) Preamble dropped the "standard self-normalized IS" overclaim — what we actually do is plain finite-sum IS with stop-gradient on $\mu_P$; self-normalization is a minibatch-level numerical-stability device handled separately at end-of-Step-3. (2) Step 2 added the absolute-continuity definition explicitly (collapses to $\mu(i)>0$ for all $i$ on finite buffer) instead of leaving it implicit; flagged $\mu(i)\propto\ell(\delta_i)$ as the zero-variance proposal. (3) Step 3 now states clearly that $\beta_t\!=\!1$ in (eq:per-anneal) recovers (eq:is-identity) verbatim, and added the per-batch max-rescaling note (Schaul Alg.1) as Step-3 footer — this is what the running code actually does. (4) Step 4: added the one-line algebra showing $\widehat{\mathcal{L}}_\text{Sem}=\mathcal{L}_U$ identically (not just in expectation) at $\beta_t\!=\!1$, the missing structural step in the previous draft. (5) Step 5: fixed a quietly-wrong claim — wrote $Z$ correctly as the $\mu_P$-weighted average semantic boost using $\sum_j\mu_P(j)=1$, bounded in $[1,w_\text{max}]$, so the under-correction factor lies in $[1, w_\text{max}^{\beta_t}]$ — clean, explicit, and exactly the constant appearing in the bias bound (eq:bias-bound). (6) New closing "Scope of the derivation" paragraph that explicitly demarcates (a)-(c) standard from (d)-(e) novel-to-this-paper, and forward-references the two-regime $q_\phi$-degeneracy split in §C.3 (the variance-amplification vs.\ signal-extinction taxonomy from the 13:35 B.3 dive).

**Verification.** Two pdflatex passes; visual gate via `pdftotext -f 28 -l 32`. Steps 1-5 + Scope render cleanly, all equations numbered, no orphaned refs. Page count unchanged (41pp). Commit on `agent/pathc-lead`. No conflict with paper-iter schedule (compile completed 15:35, next iter 15:38). No data-dependent claims touched.
